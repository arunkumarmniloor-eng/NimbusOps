apiVersion: v1
kind: Service
metadata:
  name: nimbusops-service
  labels:
    app: nimbusops
spec:
  type: NodePort
  selector:
    app: nimbusops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
